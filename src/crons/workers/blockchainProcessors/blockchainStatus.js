import _ from 'lodash';
import moment from 'moment-timezone';
import request from 'request-promise';

async function blockchainStatus() {

  ticks = []

  ticks.forEach(async function(tick) {


    tickDate = new Date(tick[0]*1000) // Close Date
    moment()
  })

  // Verify current ecurrency's value

  // Add current value to DB with weeks comparison

  //
  // Evaluate and update weeks values (minValue, maxValue, startDate, endDate)

};

export default blockchainStatus;
